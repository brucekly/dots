#!/usr/bin/osascript
if application "Music" is running then
	tell application "Music"
                if composer of current track is not "" then
                    set theArtist to composer of current track
                else
                    set theArtist to artist of current track
                end if
	 	return name of current track & "\n" & theArtist & "\n" & (player position / duration of current track) * 100
	end tell
end if
